pub struct elf_parser {
    pub number: i32,
}

impl elf_parser {
    pub fn new(num: i32) => ebpf_generator {
        ebpf_generator { number: num }
    }

    pub fn print(self) -> ! {
        println!("{:?}", number);
    }
}